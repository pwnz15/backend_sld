services:
  - type: web
    name: backend-sld
    runtime: node
    buildCommand: bash ./build.sh
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: MONGODB_URI
        sync: false
      - key: JWT_SECRET
        sync: false
    plan: free